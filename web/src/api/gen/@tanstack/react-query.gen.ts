// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, type InfiniteData, infiniteQueryOptions, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { answerRequestApiRequestsRequestIdAnswerPost, appendLogApiAgentTasksTaskIdLogPost, appendLogApiRunsRunIdLogPost, askApiAgentTasksTaskIdAskPost, cancelRunApiRunsRunIdCancelPost, deleteRunApiRunsRunIdDelete, editRunApiRunsRunIdPatch, getEventsApiRunsRunIdEventsGet, getGraphApiRunsRunIdGraphGet, getLogApiRunsRunIdLogGet, getRequestApiRequestsRequestIdGet, getRequestsApiRunsRunIdRequestsGet, getRunApiRunsRunIdGet, getSourceApiWorkflowsNameSourceGet, getStreamApiTasksTaskIdStreamGet, getTaskApiAgentTasksTaskIdGet, getTaskApiTasksTaskIdGet, getWorkflowApiWorkflowsNameGet, healthApiHealthGet, listRequestsApiRequestsGet, listRunsApiRunsGet, listWorkflowsApiWorkflowsGet, manifestApiPluginsGet, mcpAgent, meApiMeGet, moveRunApiRunsRunIdPositionPost, moveTaskApiTasksTaskIdMovePost, type Options, pauseRunApiRunsRunIdPausePost, pollRequestApiAgentTasksTaskIdRequestsRequestIdGet, rerunNodeApiRunsRunIdRerunPost, resumeRunApiRunsRunIdResumePost, retryTaskApiTasksTaskIdRetryPost, runActionApiPluginsWfActionsNamePost, setStatusApiTasksTaskIdStatusPost, submitApiAgentTasksTaskIdSubmitPost, submitRunApiWorkflowsNameRunsPost } from '../sdk.gen';
import type { AnswerRequestApiRequestsRequestIdAnswerPostData, AnswerRequestApiRequestsRequestIdAnswerPostError, AnswerRequestApiRequestsRequestIdAnswerPostResponse, AppendLogApiAgentTasksTaskIdLogPostData, AppendLogApiAgentTasksTaskIdLogPostError, AppendLogApiAgentTasksTaskIdLogPostResponse, AppendLogApiRunsRunIdLogPostData, AppendLogApiRunsRunIdLogPostError, AppendLogApiRunsRunIdLogPostResponse, AskApiAgentTasksTaskIdAskPostData, AskApiAgentTasksTaskIdAskPostError, AskApiAgentTasksTaskIdAskPostResponse, CancelRunApiRunsRunIdCancelPostData, CancelRunApiRunsRunIdCancelPostError, CancelRunApiRunsRunIdCancelPostResponse, DeleteRunApiRunsRunIdDeleteData, DeleteRunApiRunsRunIdDeleteError, DeleteRunApiRunsRunIdDeleteResponse, EditRunApiRunsRunIdPatchData, EditRunApiRunsRunIdPatchError, EditRunApiRunsRunIdPatchResponse, GetEventsApiRunsRunIdEventsGetData, GetEventsApiRunsRunIdEventsGetError, GetEventsApiRunsRunIdEventsGetResponse, GetGraphApiRunsRunIdGraphGetData, GetGraphApiRunsRunIdGraphGetError, GetGraphApiRunsRunIdGraphGetResponse, GetLogApiRunsRunIdLogGetData, GetLogApiRunsRunIdLogGetError, GetLogApiRunsRunIdLogGetResponse, GetRequestApiRequestsRequestIdGetData, GetRequestApiRequestsRequestIdGetError, GetRequestApiRequestsRequestIdGetResponse, GetRequestsApiRunsRunIdRequestsGetData, GetRequestsApiRunsRunIdRequestsGetError, GetRequestsApiRunsRunIdRequestsGetResponse, GetRunApiRunsRunIdGetData, GetRunApiRunsRunIdGetError, GetRunApiRunsRunIdGetResponse, GetSourceApiWorkflowsNameSourceGetData, GetSourceApiWorkflowsNameSourceGetError, GetSourceApiWorkflowsNameSourceGetResponse, GetStreamApiTasksTaskIdStreamGetData, GetStreamApiTasksTaskIdStreamGetError, GetStreamApiTasksTaskIdStreamGetResponse, GetTaskApiAgentTasksTaskIdGetData, GetTaskApiAgentTasksTaskIdGetError, GetTaskApiAgentTasksTaskIdGetResponse, GetTaskApiTasksTaskIdGetData, GetTaskApiTasksTaskIdGetError, GetTaskApiTasksTaskIdGetResponse, GetWorkflowApiWorkflowsNameGetData, GetWorkflowApiWorkflowsNameGetError, GetWorkflowApiWorkflowsNameGetResponse, HealthApiHealthGetData, HealthApiHealthGetResponse, ListRequestsApiRequestsGetData, ListRequestsApiRequestsGetError, ListRequestsApiRequestsGetResponse, ListRunsApiRunsGetData, ListRunsApiRunsGetError, ListRunsApiRunsGetResponse, ListWorkflowsApiWorkflowsGetData, ListWorkflowsApiWorkflowsGetError, ListWorkflowsApiWorkflowsGetResponse, ManifestApiPluginsGetData, ManifestApiPluginsGetError, ManifestApiPluginsGetResponse, McpAgentData, McpAgentError, MeApiMeGetData, MeApiMeGetResponse, MoveRunApiRunsRunIdPositionPostData, MoveRunApiRunsRunIdPositionPostError, MoveRunApiRunsRunIdPositionPostResponse, MoveTaskApiTasksTaskIdMovePostData, MoveTaskApiTasksTaskIdMovePostError, MoveTaskApiTasksTaskIdMovePostResponse, PauseRunApiRunsRunIdPausePostData, PauseRunApiRunsRunIdPausePostError, PauseRunApiRunsRunIdPausePostResponse, PollRequestApiAgentTasksTaskIdRequestsRequestIdGetData, PollRequestApiAgentTasksTaskIdRequestsRequestIdGetError, PollRequestApiAgentTasksTaskIdRequestsRequestIdGetResponse, RerunNodeApiRunsRunIdRerunPostData, RerunNodeApiRunsRunIdRerunPostError, RerunNodeApiRunsRunIdRerunPostResponse, ResumeRunApiRunsRunIdResumePostData, ResumeRunApiRunsRunIdResumePostError, ResumeRunApiRunsRunIdResumePostResponse, RetryTaskApiTasksTaskIdRetryPostData, RetryTaskApiTasksTaskIdRetryPostError, RetryTaskApiTasksTaskIdRetryPostResponse, RunActionApiPluginsWfActionsNamePostData, RunActionApiPluginsWfActionsNamePostError, SetStatusApiTasksTaskIdStatusPostData, SetStatusApiTasksTaskIdStatusPostError, SetStatusApiTasksTaskIdStatusPostResponse, SubmitApiAgentTasksTaskIdSubmitPostData, SubmitApiAgentTasksTaskIdSubmitPostError, SubmitApiAgentTasksTaskIdSubmitPostResponse, SubmitRunApiWorkflowsNameRunsPostData, SubmitRunApiWorkflowsNameRunsPostError, SubmitRunApiWorkflowsNameRunsPostResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const getTaskApiAgentTasksTaskIdGetQueryKey = (options: Options<GetTaskApiAgentTasksTaskIdGetData>) => createQueryKey('getTaskApiAgentTasksTaskIdGet', options);

/**
 * Read this task: its brief and the work log
 *
 * Everything the agent needs to start, and nothing else (08).
 *
 * The title and the description are the run's — the brief an operator
 * submitted — and ``input`` is the payload this node was enqueued with,
 * ``null`` when it had none. ``log`` is the run's work log, oldest
 * first, uncapped and untruncated: it is the inter-stage channel (D4),
 * it is bounded by the number of stages rather than by agent output,
 * and a retried attempt reads the ``failure`` entry that says why the
 * last one failed.
 *
 * The ``stats`` entries are dropped (D56). ``output_schema`` is here
 * only while a façade has declared an ``output_model`` on this
 * attempt's live context: the ``native`` tooling tier reads it to build
 * its ``submit_result`` tool (05 §Tooling tiers), and an agent with no
 * model declared is not required to submit anything.
 */
export const getTaskApiAgentTasksTaskIdGetOptions = (options: Options<GetTaskApiAgentTasksTaskIdGetData>) => queryOptions<GetTaskApiAgentTasksTaskIdGetResponse, GetTaskApiAgentTasksTaskIdGetError, GetTaskApiAgentTasksTaskIdGetResponse, ReturnType<typeof getTaskApiAgentTasksTaskIdGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTaskApiAgentTasksTaskIdGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTaskApiAgentTasksTaskIdGetQueryKey(options)
});

/**
 * Ask the operator a question
 *
 * Open a request against this attempt, if the agent may ask at all.
 *
 * 403 unless the façade running this attempt declared
 * ``ask_policy="http"`` (05 §Policies): an agent cannot grant itself
 * the right to interrupt a person, and the policy lives on the agent
 * class the workflow chose. The prompt sections that tell an agent how
 * to ask are omitted when it is off (19), so a 403 here is an agent
 * that went looking.
 *
 * What was sent decides the mode: ``options`` for a pick-one question,
 * ``schema`` for a form — whose answer is validated against that very
 * schema where it lands, by the validator registered here (06
 * §Service) — and neither for free text. The request is opened with
 * ``source="agent"`` and no ordinal, because a re-executed attempt does
 * not reproduce a turn statement for statement (06 §Restart
 * durability).
 *
 * The answer does not come back here: the caller polls
 * ``/requests/{rid}``, which is what lets the agent keep its turn
 * rather than holding this connection open.
 */
export const askApiAgentTasksTaskIdAskPostMutation = (options?: Partial<Options<AskApiAgentTasksTaskIdAskPostData>>): UseMutationOptions<AskApiAgentTasksTaskIdAskPostResponse, AskApiAgentTasksTaskIdAskPostError, Options<AskApiAgentTasksTaskIdAskPostData>> => {
    const mutationOptions: UseMutationOptions<AskApiAgentTasksTaskIdAskPostResponse, AskApiAgentTasksTaskIdAskPostError, Options<AskApiAgentTasksTaskIdAskPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await askApiAgentTasksTaskIdAskPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Append this attempt's deliverable
 *
 * Write one entry to the run's work log, author ``agent``.
 *
 * The deliverable of a stage, which is what the next stage reads (D4).
 * It is written through the attempt's own
 * :class:`~athanore.engine.services.LogService`, so it carries this
 * task's id and node and emits the ``log.appended`` the SPA re-renders
 * on — the same call a node body makes, reached over HTTP.
 */
export const appendLogApiAgentTasksTaskIdLogPostMutation = (options?: Partial<Options<AppendLogApiAgentTasksTaskIdLogPostData>>): UseMutationOptions<AppendLogApiAgentTasksTaskIdLogPostResponse, AppendLogApiAgentTasksTaskIdLogPostError, Options<AppendLogApiAgentTasksTaskIdLogPostData>> => {
    const mutationOptions: UseMutationOptions<AppendLogApiAgentTasksTaskIdLogPostResponse, AppendLogApiAgentTasksTaskIdLogPostError, Options<AppendLogApiAgentTasksTaskIdLogPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await appendLogApiAgentTasksTaskIdLogPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const pollRequestApiAgentTasksTaskIdRequestsRequestIdGetQueryKey = (options: Options<PollRequestApiAgentTasksTaskIdRequestsRequestIdGetData>) => createQueryKey('pollRequestApiAgentTasksTaskIdRequestsRequestIdGet', options);

/**
 * Wait for the answer to a question
 *
 * The answer, waiting up to ``wait`` seconds for one to land.
 *
 * ``wait`` is clamped to :data:`MAX_WAIT` rather than refused, so 19's
 * instruction — poll with ``?wait=60`` and repeat until it says
 * ``answered`` — is a loop that costs one request per minute and not a
 * spin. The wait ends the moment the answer is recorded, not when the
 * clamp expires.
 *
 * A request of **another** task is a 404, not someone else's answer: a
 * token names one attempt and this route reads only what that attempt
 * opened (12 §Task tokens).
 *
 * Nothing is claimed. An agent that lost a response and asked again
 * gets the same answer again, which is what makes a dropped connection
 * cost nothing (06 §Service).
 */
export const pollRequestApiAgentTasksTaskIdRequestsRequestIdGetOptions = (options: Options<PollRequestApiAgentTasksTaskIdRequestsRequestIdGetData>) => queryOptions<PollRequestApiAgentTasksTaskIdRequestsRequestIdGetResponse, PollRequestApiAgentTasksTaskIdRequestsRequestIdGetError, PollRequestApiAgentTasksTaskIdRequestsRequestIdGetResponse, ReturnType<typeof pollRequestApiAgentTasksTaskIdRequestsRequestIdGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await pollRequestApiAgentTasksTaskIdRequestsRequestIdGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: pollRequestApiAgentTasksTaskIdRequestsRequestIdGetQueryKey(options)
});

/**
 * Submit this task's structured result
 *
 * Record a value for this attempt, if it fits what was asked for.
 *
 * With an ``output_model`` declared, a misfit is a 422 carrying
 * ``errors`` and ``schema`` and nothing is stored: the agent reads the
 * two in its own tool output and can fix the shape inside the same turn
 * (05 §Submissions). The rejection is also left on the context as
 * ``ctx.last_rejection``, which is what 19's repair turn quotes back if
 * the turn ends without a valid submission.
 *
 * With none declared, any JSON is stored as it arrived.
 *
 * Submitting twice is not an error: the **latest valid** payload wins
 * (D5), and a body reads it with ``submissions.latest()``. A submission
 * never routes anything — the node body decides — so an agent cannot
 * move its own task.
 */
export const submitApiAgentTasksTaskIdSubmitPostMutation = (options?: Partial<Options<SubmitApiAgentTasksTaskIdSubmitPostData>>): UseMutationOptions<SubmitApiAgentTasksTaskIdSubmitPostResponse, SubmitApiAgentTasksTaskIdSubmitPostError, Options<SubmitApiAgentTasksTaskIdSubmitPostData>> => {
    const mutationOptions: UseMutationOptions<SubmitApiAgentTasksTaskIdSubmitPostResponse, SubmitApiAgentTasksTaskIdSubmitPostError, Options<SubmitApiAgentTasksTaskIdSubmitPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await submitApiAgentTasksTaskIdSubmitPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const healthApiHealthGetQueryKey = (options?: Options<HealthApiHealthGetData>) => createQueryKey('healthApiHealthGet', options);

/**
 * Liveness and version
 *
 * Whether the server is up, which version it is, and what it is doing.
 *
 * Pausing every run and then waiting for `tasks_in_progress` to reach
 * zero here is how an operator drains a server before stopping it.
 */
export const healthApiHealthGetOptions = (options?: Options<HealthApiHealthGetData>) => queryOptions<HealthApiHealthGetResponse, DefaultError, HealthApiHealthGetResponse, ReturnType<typeof healthApiHealthGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await healthApiHealthGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: healthApiHealthGetQueryKey(options)
});

export const meApiMeGetQueryKey = (options?: Options<MeApiMeGetData>) => createQueryKey('meApiMeGet', options);

/**
 * Whether the caller needs a token, and has one
 *
 * Whether this server wants a token, and whether this request has one.
 *
 * Never requires one itself: a request with no credential on a server
 * that wants one gets a 200 saying `{"auth": "token", "authenticated":
 * false}`, which is the client's cue to ask for a token.
 */
export const meApiMeGetOptions = (options?: Options<MeApiMeGetData>) => queryOptions<MeApiMeGetResponse, DefaultError, MeApiMeGetResponse, ReturnType<typeof meApiMeGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await meApiMeGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: meApiMeGetQueryKey(options)
});

export const manifestApiPluginsGetQueryKey = (options?: Options<ManifestApiPluginsGetData>) => createQueryKey('manifestApiPluginsGet', options);

/**
 * Everything the registered workflows contribute to the UI
 *
 * The plugin manifest: panels, actions and assets, per workflow.
 *
 * Builtins first, then the workflows in registration order. The
 * SPA fetches this at boot and again whenever the SSE stream
 * reconnects onto a server with a new ``started_at``, because a
 * manifest changes only when the process does (09 §Wire contract).
 */
export const manifestApiPluginsGetOptions = (options?: Options<ManifestApiPluginsGetData>) => queryOptions<ManifestApiPluginsGetResponse, ManifestApiPluginsGetError, ManifestApiPluginsGetResponse, ReturnType<typeof manifestApiPluginsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await manifestApiPluginsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: manifestApiPluginsGetQueryKey(options)
});

/**
 * Run one of a workflow's declared actions
 *
 * Validate the input, resolve the scope, run the handler.
 *
 * The one way an action is invoked (09 §Wire contract). It answers
 * with whatever the handler returned, as JSON.
 */
export const runActionApiPluginsWfActionsNamePostMutation = (options?: Partial<Options<RunActionApiPluginsWfActionsNamePostData>>): UseMutationOptions<unknown, RunActionApiPluginsWfActionsNamePostError, Options<RunActionApiPluginsWfActionsNamePostData>> => {
    const mutationOptions: UseMutationOptions<unknown, RunActionApiPluginsWfActionsNamePostError, Options<RunActionApiPluginsWfActionsNamePostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await runActionApiPluginsWfActionsNamePost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const listRequestsApiRequestsGetQueryKey = (options?: Options<ListRequestsApiRequestsGetData>) => createQueryKey('listRequestsApiRequestsGet', options);

/**
 * The inbox: every request still waiting on a person
 *
 * The requests of every run, or of one, oldest first.
 *
 * "Pending" is narrower than "unanswered": a request whose task has
 * ended is **stale** and leaves the inbox, because an answer to it
 * would reach nobody (06 §Restart durability). It stays in the run's
 * own history, and ``pending=false`` here shows it too, with
 * ``stale: true`` on it.
 *
 * An application with no store holds no requests and answers with an
 * empty list; a ``run`` that names no run is a filter that matches
 * nothing rather than a 404.
 */
export const listRequestsApiRequestsGetOptions = (options?: Options<ListRequestsApiRequestsGetData>) => queryOptions<ListRequestsApiRequestsGetResponse, ListRequestsApiRequestsGetError, ListRequestsApiRequestsGetResponse, ReturnType<typeof listRequestsApiRequestsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listRequestsApiRequestsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listRequestsApiRequestsGetQueryKey(options)
});

export const getRequestApiRequestsRequestIdGetQueryKey = (options: Options<GetRequestApiRequestsRequestIdGetData>) => createQueryKey('getRequestApiRequestsRequestIdGet', options);

/**
 * One request and its answer
 *
 * One request, answered or not, with the node that asked.
 */
export const getRequestApiRequestsRequestIdGetOptions = (options: Options<GetRequestApiRequestsRequestIdGetData>) => queryOptions<GetRequestApiRequestsRequestIdGetResponse, GetRequestApiRequestsRequestIdGetError, GetRequestApiRequestsRequestIdGetResponse, ReturnType<typeof getRequestApiRequestsRequestIdGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRequestApiRequestsRequestIdGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRequestApiRequestsRequestIdGetQueryKey(options)
});

/**
 * Answer a request
 *
 * Record the one answer this request may have, and return it.
 *
 * ``option_id`` for an ``options`` request and ``value`` for a ``text``
 * or ``form`` one; the request's own ``mode`` decides which of the two
 * is read, so a ``value`` sent to an ``options`` request is not an
 * answer that request could have (06 §Service).
 *
 * The response is the **updated** view rather than the answer row (08
 * §Requests): the SPA re-renders the card it just answered, and
 * ``pending``, ``answer`` and ``answered_by`` are all part of what
 * changed. The author is ``user`` — this route is the operator's, and
 * the other author, ``engine``, belongs to the headless fallbacks that
 * record an answer without a person (06 §Timeouts).
 */
export const answerRequestApiRequestsRequestIdAnswerPostMutation = (options?: Partial<Options<AnswerRequestApiRequestsRequestIdAnswerPostData>>): UseMutationOptions<AnswerRequestApiRequestsRequestIdAnswerPostResponse, AnswerRequestApiRequestsRequestIdAnswerPostError, Options<AnswerRequestApiRequestsRequestIdAnswerPostData>> => {
    const mutationOptions: UseMutationOptions<AnswerRequestApiRequestsRequestIdAnswerPostResponse, AnswerRequestApiRequestsRequestIdAnswerPostError, Options<AnswerRequestApiRequestsRequestIdAnswerPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await answerRequestApiRequestsRequestIdAnswerPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const listRunsApiRunsGetQueryKey = (options?: Options<ListRunsApiRunsGetData>) => createQueryKey('listRunsApiRunsGet', options);

/**
 * Every run, in dispatch order
 *
 * The run list of 08 §Runs, whole.
 *
 * Run lists are small and return whole (08 §Conventions). The order is
 * the dispatch order — ``position`` ascending — so the list the
 * operator reads is the order the scheduler will claim in.
 *
 * An application with no store has no runs and answers with an empty
 * list: "nothing to ask" and "nothing queued" are the same fact for a
 * server that holds no work, unlike ``/api/health``'s counts.
 */
export const listRunsApiRunsGetOptions = (options?: Options<ListRunsApiRunsGetData>) => queryOptions<ListRunsApiRunsGetResponse, ListRunsApiRunsGetError, ListRunsApiRunsGetResponse, ReturnType<typeof listRunsApiRunsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listRunsApiRunsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listRunsApiRunsGetQueryKey(options)
});

/**
 * Delete a run and everything under it
 *
 * Cancel what is outstanding, then remove the run.
 *
 * Both halves are ``Ops.delete``'s, in that order and for its reason:
 * the attempts are killed against rows that still exist.
 */
export const deleteRunApiRunsRunIdDeleteMutation = (options?: Partial<Options<DeleteRunApiRunsRunIdDeleteData>>): UseMutationOptions<DeleteRunApiRunsRunIdDeleteResponse, DeleteRunApiRunsRunIdDeleteError, Options<DeleteRunApiRunsRunIdDeleteData>> => {
    const mutationOptions: UseMutationOptions<DeleteRunApiRunsRunIdDeleteResponse, DeleteRunApiRunsRunIdDeleteError, Options<DeleteRunApiRunsRunIdDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await deleteRunApiRunsRunIdDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getRunApiRunsRunIdGetQueryKey = (options: Options<GetRunApiRunsRunIdGetData>) => createQueryKey('getRunApiRunsRunIdGet', options);

/**
 * One run, its attempts and its totals
 *
 * A run with everything the overview pane draws (08 §Runs).
 *
 * ``current_nodes`` and ``pending_requests`` are the list query's two
 * derived fields, recomputed from this read: the attempts are already
 * in hand, so the in-flight nodes cost nothing, and the pending
 * requests are one indexed query over this run alone.
 */
export const getRunApiRunsRunIdGetOptions = (options: Options<GetRunApiRunsRunIdGetData>) => queryOptions<GetRunApiRunsRunIdGetResponse, GetRunApiRunsRunIdGetError, GetRunApiRunsRunIdGetResponse, ReturnType<typeof getRunApiRunsRunIdGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRunApiRunsRunIdGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRunApiRunsRunIdGetQueryKey(options)
});

/**
 * Change a run's title or description
 *
 * Write the fields the body names, and answer with the run.
 *
 * A field the body omits is left alone, which is why an edit that
 * names neither is accepted and changes nothing (``Ops.edit``). The
 * response is the whole detail rather than the edited row: the SPA
 * re-renders the overview from it, and a second GET to get the
 * attempts back would be a round trip for nothing.
 */
export const editRunApiRunsRunIdPatchMutation = (options?: Partial<Options<EditRunApiRunsRunIdPatchData>>): UseMutationOptions<EditRunApiRunsRunIdPatchResponse, EditRunApiRunsRunIdPatchError, Options<EditRunApiRunsRunIdPatchData>> => {
    const mutationOptions: UseMutationOptions<EditRunApiRunsRunIdPatchResponse, EditRunApiRunsRunIdPatchError, Options<EditRunApiRunsRunIdPatchData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await editRunApiRunsRunIdPatch({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * End a run and everything under it
 *
 * Cancel the run and every outstanding attempt; 409 if it has ended.
 *
 * The ``note`` names how many attempts were stopped, because that is
 * the part of the outcome the request could not predict: a run with
 * nothing in flight and a run with three agents mid-turn are the same
 * call and very different events. It is omitted when there were none,
 * rather than sent as "0 attempts".
 */
export const cancelRunApiRunsRunIdCancelPostMutation = (options?: Partial<Options<CancelRunApiRunsRunIdCancelPostData>>): UseMutationOptions<CancelRunApiRunsRunIdCancelPostResponse, CancelRunApiRunsRunIdCancelPostError, Options<CancelRunApiRunsRunIdCancelPostData>> => {
    const mutationOptions: UseMutationOptions<CancelRunApiRunsRunIdCancelPostResponse, CancelRunApiRunsRunIdCancelPostError, Options<CancelRunApiRunsRunIdCancelPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await cancelRunApiRunsRunIdCancelPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getEventsApiRunsRunIdEventsGetQueryKey = (options: Options<GetEventsApiRunsRunIdEventsGetData>) => createQueryKey('getEventsApiRunsRunIdEventsGet', options);

/**
 * A run's stored events
 *
 * One page of the run's history, oldest first.
 *
 * The same typed envelope the SSE feed sends (18 §Typing), so a client
 * that caught up from here and then subscribed switches sources
 * without switching shapes. `task.stream` is ephemeral and is never
 * stored, so it never appears in a page.
 */
export const getEventsApiRunsRunIdEventsGetOptions = (options: Options<GetEventsApiRunsRunIdEventsGetData>) => queryOptions<GetEventsApiRunsRunIdEventsGetResponse, GetEventsApiRunsRunIdEventsGetError, GetEventsApiRunsRunIdEventsGetResponse, ReturnType<typeof getEventsApiRunsRunIdEventsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getEventsApiRunsRunIdEventsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getEventsApiRunsRunIdEventsGetQueryKey(options)
});

const createInfiniteParams = <K extends Pick<QueryKey<Options>[0], 'body' | 'headers' | 'path' | 'query'>>(queryKey: QueryKey<Options>, page: K) => {
    const params = { ...queryKey[0] };
    if (page.body) {
        params.body = {
            ...queryKey[0].body as any,
            ...page.body as any
        };
    }
    if (page.headers) {
        params.headers = {
            ...queryKey[0].headers,
            ...page.headers
        };
    }
    if (page.path) {
        params.path = {
            ...queryKey[0].path as any,
            ...page.path as any
        };
    }
    if (page.query) {
        params.query = {
            ...queryKey[0].query as any,
            ...page.query as any
        };
    }
    return params as unknown as typeof page;
};

export const getEventsApiRunsRunIdEventsGetInfiniteQueryKey = (options: Options<GetEventsApiRunsRunIdEventsGetData>): QueryKey<Options<GetEventsApiRunsRunIdEventsGetData>> => createQueryKey('getEventsApiRunsRunIdEventsGet', options, true);

/**
 * A run's stored events
 *
 * One page of the run's history, oldest first.
 *
 * The same typed envelope the SSE feed sends (18 §Typing), so a client
 * that caught up from here and then subscribed switches sources
 * without switching shapes. `task.stream` is ephemeral and is never
 * stored, so it never appears in a page.
 */
export const getEventsApiRunsRunIdEventsGetInfiniteOptions = (options: Options<GetEventsApiRunsRunIdEventsGetData>) => {
    const opts = infiniteQueryOptions<GetEventsApiRunsRunIdEventsGetResponse, GetEventsApiRunsRunIdEventsGetError, InfiniteData<GetEventsApiRunsRunIdEventsGetResponse>, QueryKey<Options<GetEventsApiRunsRunIdEventsGetData>>, number | Pick<QueryKey<Options<GetEventsApiRunsRunIdEventsGetData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetEventsApiRunsRunIdEventsGetData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    after: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getEventsApiRunsRunIdEventsGet({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getEventsApiRunsRunIdEventsGetInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

export const getGraphApiRunsRunIdGraphGetQueryKey = (options: Options<GetGraphApiRunsRunIdGraphGetData>) => createQueryKey('getGraphApiRunsRunIdGraphGet', options);

/**
 * The workflow's graph, for this run
 *
 * The registered graph with this run's history projected onto it.
 *
 * A run whose workflow this server does not have registered is a 404
 * ``unknown_workflow``: there is no graph to project onto, and the run
 * list already says so with ``unregistered`` rather than pretending a
 * shape.
 */
export const getGraphApiRunsRunIdGraphGetOptions = (options: Options<GetGraphApiRunsRunIdGraphGetData>) => queryOptions<GetGraphApiRunsRunIdGraphGetResponse, GetGraphApiRunsRunIdGraphGetError, GetGraphApiRunsRunIdGraphGetResponse, ReturnType<typeof getGraphApiRunsRunIdGraphGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getGraphApiRunsRunIdGraphGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getGraphApiRunsRunIdGraphGetQueryKey(options)
});

export const getLogApiRunsRunIdLogGetQueryKey = (options: Options<GetLogApiRunsRunIdLogGetData>) => createQueryKey('getLogApiRunsRunIdLogGet', options);

/**
 * A run's work log
 *
 * Every entry of the work log, oldest first and never truncated.
 *
 * Whole, and unpaged: the log is bounded by the number of stages
 * rather than by agent output (08 §Agent-facing). The `stats` entries
 * an agent never sees are here — token counts are operator
 * information.
 */
export const getLogApiRunsRunIdLogGetOptions = (options: Options<GetLogApiRunsRunIdLogGetData>) => queryOptions<GetLogApiRunsRunIdLogGetResponse, GetLogApiRunsRunIdLogGetError, GetLogApiRunsRunIdLogGetResponse, ReturnType<typeof getLogApiRunsRunIdLogGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getLogApiRunsRunIdLogGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getLogApiRunsRunIdLogGetQueryKey(options)
});

/**
 * Append an operator note to the work log
 *
 * Write a `user` entry under the run's current node.
 *
 * The node is the one node with an attempt in flight, and ``user``
 * when there is not exactly one: a note filed under one branch of a
 * fan-out would claim a context it does not have (``Ops.append_log``).
 */
export const appendLogApiRunsRunIdLogPostMutation = (options?: Partial<Options<AppendLogApiRunsRunIdLogPostData>>): UseMutationOptions<AppendLogApiRunsRunIdLogPostResponse, AppendLogApiRunsRunIdLogPostError, Options<AppendLogApiRunsRunIdLogPostData>> => {
    const mutationOptions: UseMutationOptions<AppendLogApiRunsRunIdLogPostResponse, AppendLogApiRunsRunIdLogPostError, Options<AppendLogApiRunsRunIdLogPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await appendLogApiRunsRunIdLogPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Stop a run dispatching
 *
 * Pause a `queued` or `running` run; 409 on anything else.
 *
 * Pause is about the **next** task: an attempt already in flight runs
 * to its end and enqueues its successor, and that successor waits (04
 * §Operator operations). Killing running work is ``/cancel``.
 */
export const pauseRunApiRunsRunIdPausePostMutation = (options?: Partial<Options<PauseRunApiRunsRunIdPausePostData>>): UseMutationOptions<PauseRunApiRunsRunIdPausePostResponse, PauseRunApiRunsRunIdPausePostError, Options<PauseRunApiRunsRunIdPausePostData>> => {
    const mutationOptions: UseMutationOptions<PauseRunApiRunsRunIdPausePostResponse, PauseRunApiRunsRunIdPausePostError, Options<PauseRunApiRunsRunIdPausePostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await pauseRunApiRunsRunIdPausePost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Move a run in the dispatch list
 *
 * Swap with a neighbour, or move to a zero-based index (D57).
 *
 * Both ends are a no-op that still answers 200 with the position the
 * run already had, and an ``index`` outside the list is clamped to it:
 * "top" is `{"index": 0}` and there is nothing for the caller to
 * bounds-check. The body model has already refused neither-or-both of
 * the two fields.
 */
export const moveRunApiRunsRunIdPositionPostMutation = (options?: Partial<Options<MoveRunApiRunsRunIdPositionPostData>>): UseMutationOptions<MoveRunApiRunsRunIdPositionPostResponse, MoveRunApiRunsRunIdPositionPostError, Options<MoveRunApiRunsRunIdPositionPostData>> => {
    const mutationOptions: UseMutationOptions<MoveRunApiRunsRunIdPositionPostResponse, MoveRunApiRunsRunIdPositionPostError, Options<MoveRunApiRunsRunIdPositionPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await moveRunApiRunsRunIdPositionPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getRequestsApiRunsRunIdRequestsGetQueryKey = (options: Options<GetRequestsApiRunsRunIdRequestsGetData>) => createQueryKey('getRequestsApiRunsRunIdRequestsGet', options);

/**
 * A run's requests
 *
 * Every request the run opened, answered or not, oldest first.
 *
 * The run's history rather than the inbox: a stale request — one whose
 * attempt is gone — stays here and leaves ``/api/requests?pending=true``
 * (06 §Restart durability).
 */
export const getRequestsApiRunsRunIdRequestsGetOptions = (options: Options<GetRequestsApiRunsRunIdRequestsGetData>) => queryOptions<GetRequestsApiRunsRunIdRequestsGetResponse, GetRequestsApiRunsRunIdRequestsGetError, GetRequestsApiRunsRunIdRequestsGetResponse, ReturnType<typeof getRequestsApiRunsRunIdRequestsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getRequestsApiRunsRunIdRequestsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getRequestsApiRunsRunIdRequestsGetQueryKey(options)
});

/**
 * Run a node again
 *
 * Enqueue a fresh attempt of ``node`` with the payload it last had.
 *
 * A join replays the arrivals the store holds rather than the payload
 * its task was given, which is what makes a rerun the remedy for a
 * branch that arrived late (04 §Failure and operator semantics).
 */
export const rerunNodeApiRunsRunIdRerunPostMutation = (options?: Partial<Options<RerunNodeApiRunsRunIdRerunPostData>>): UseMutationOptions<RerunNodeApiRunsRunIdRerunPostResponse, RerunNodeApiRunsRunIdRerunPostError, Options<RerunNodeApiRunsRunIdRerunPostData>> => {
    const mutationOptions: UseMutationOptions<RerunNodeApiRunsRunIdRerunPostResponse, RerunNodeApiRunsRunIdRerunPostError, Options<RerunNodeApiRunsRunIdRerunPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await rerunNodeApiRunsRunIdRerunPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Let a paused run dispatch again
 *
 * Resume a `paused` run; 409 on anything else.
 */
export const resumeRunApiRunsRunIdResumePostMutation = (options?: Partial<Options<ResumeRunApiRunsRunIdResumePostData>>): UseMutationOptions<ResumeRunApiRunsRunIdResumePostResponse, ResumeRunApiRunsRunIdResumePostError, Options<ResumeRunApiRunsRunIdResumePostData>> => {
    const mutationOptions: UseMutationOptions<ResumeRunApiRunsRunIdResumePostResponse, ResumeRunApiRunsRunIdResumePostError, Options<ResumeRunApiRunsRunIdResumePostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await resumeRunApiRunsRunIdResumePost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getTaskApiTasksTaskIdGetQueryKey = (options: Options<GetTaskApiTasksTaskIdGetData>) => createQueryKey('getTaskApiTasksTaskIdGet', options);

/**
 * One attempt and what was submitted for it
 *
 * The operator view of a task: 08's ``TaskRow`` plus ``submissions``.
 *
 * Every accepted submission, oldest first, because "last valid wins"
 * (D5) is a rule about which one the body reads and not about which
 * ones happened: an operator reading a repaired attempt wants the
 * rejected shape and the accepted one.
 *
 * No token, in this response or any other: a task token is
 * header-only (12 §Task tokens), and ``TaskDetail`` has no field that
 * could carry one.
 */
export const getTaskApiTasksTaskIdGetOptions = (options: Options<GetTaskApiTasksTaskIdGetData>) => queryOptions<GetTaskApiTasksTaskIdGetResponse, GetTaskApiTasksTaskIdGetError, GetTaskApiTasksTaskIdGetResponse, ReturnType<typeof getTaskApiTasksTaskIdGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getTaskApiTasksTaskIdGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getTaskApiTasksTaskIdGetQueryKey(options)
});

/**
 * Move a task's work to another node
 *
 * Cancel the attempt and enqueue its payload at ``node``.
 *
 * 409 ``conflict`` when ``node`` is a **join** (04 §Fan-in): a join is
 * dispatched by its arrivals and called with all of them, so a task
 * moved into one would be a join attempt holding a single branch's
 * payload, with no arrival recorded and a run left waiting for
 * branches that already landed.
 */
export const moveTaskApiTasksTaskIdMovePostMutation = (options?: Partial<Options<MoveTaskApiTasksTaskIdMovePostData>>): UseMutationOptions<MoveTaskApiTasksTaskIdMovePostResponse, MoveTaskApiTasksTaskIdMovePostError, Options<MoveTaskApiTasksTaskIdMovePostData>> => {
    const mutationOptions: UseMutationOptions<MoveTaskApiTasksTaskIdMovePostResponse, MoveTaskApiTasksTaskIdMovePostError, Options<MoveTaskApiTasksTaskIdMovePostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await moveTaskApiTasksTaskIdMovePost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Queue another attempt of a task
 *
 * Enqueue the next attempt of a task that has stopped.
 *
 * Same node, same payload, same branch and the same ``created``, so a
 * retry keeps its place in the dispatch order (``Ops.retry``). Refused
 * with 409 ``conflict`` while the task is still going: a second attempt
 * of a task that already has one is two attempts of one task.
 */
export const retryTaskApiTasksTaskIdRetryPostMutation = (options?: Partial<Options<RetryTaskApiTasksTaskIdRetryPostData>>): UseMutationOptions<RetryTaskApiTasksTaskIdRetryPostResponse, RetryTaskApiTasksTaskIdRetryPostError, Options<RetryTaskApiTasksTaskIdRetryPostData>> => {
    const mutationOptions: UseMutationOptions<RetryTaskApiTasksTaskIdRetryPostResponse, RetryTaskApiTasksTaskIdRetryPostError, Options<RetryTaskApiTasksTaskIdRetryPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await retryTaskApiTasksTaskIdRetryPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Write a task's status
 *
 * Put the task in one of the three statuses an operator may write.
 *
 * ``ready`` re-dispatches it and re-opens a terminal run, ``cancelled``
 * stops it and ``dead_letter`` files it as failed for good; the other
 * four statuses are the engine's record of what happened and are not an
 * operator's to declare, which is why the body's field is a
 * ``Literal`` and a fourth value is a 422 before the engine is reached.
 */
export const setStatusApiTasksTaskIdStatusPostMutation = (options?: Partial<Options<SetStatusApiTasksTaskIdStatusPostData>>): UseMutationOptions<SetStatusApiTasksTaskIdStatusPostResponse, SetStatusApiTasksTaskIdStatusPostError, Options<SetStatusApiTasksTaskIdStatusPostData>> => {
    const mutationOptions: UseMutationOptions<SetStatusApiTasksTaskIdStatusPostResponse, SetStatusApiTasksTaskIdStatusPostError, Options<SetStatusApiTasksTaskIdStatusPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await setStatusApiTasksTaskIdStatusPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getStreamApiTasksTaskIdStreamGetQueryKey = (options: Options<GetStreamApiTasksTaskIdStreamGetData>) => createQueryKey('getStreamApiTasksTaskIdStreamGet', options);

/**
 * A page of a task's agent transcript
 *
 * The chunks after ``after``, in transcript order (08 §Tasks).
 *
 * ``after=0`` is the whole transcript from the start, which is what a
 * tab opening on a finished attempt asks for; a client following a
 * live one passes the ``seq_to`` of the ephemeral ``task.stream`` event
 * it just received (18) and appends what comes back.
 */
export const getStreamApiTasksTaskIdStreamGetOptions = (options: Options<GetStreamApiTasksTaskIdStreamGetData>) => queryOptions<GetStreamApiTasksTaskIdStreamGetResponse, GetStreamApiTasksTaskIdStreamGetError, GetStreamApiTasksTaskIdStreamGetResponse, ReturnType<typeof getStreamApiTasksTaskIdStreamGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getStreamApiTasksTaskIdStreamGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getStreamApiTasksTaskIdStreamGetQueryKey(options)
});

export const getStreamApiTasksTaskIdStreamGetInfiniteQueryKey = (options: Options<GetStreamApiTasksTaskIdStreamGetData>): QueryKey<Options<GetStreamApiTasksTaskIdStreamGetData>> => createQueryKey('getStreamApiTasksTaskIdStreamGet', options, true);

/**
 * A page of a task's agent transcript
 *
 * The chunks after ``after``, in transcript order (08 §Tasks).
 *
 * ``after=0`` is the whole transcript from the start, which is what a
 * tab opening on a finished attempt asks for; a client following a
 * live one passes the ``seq_to`` of the ephemeral ``task.stream`` event
 * it just received (18) and appends what comes back.
 */
export const getStreamApiTasksTaskIdStreamGetInfiniteOptions = (options: Options<GetStreamApiTasksTaskIdStreamGetData>) => {
    const opts = infiniteQueryOptions<GetStreamApiTasksTaskIdStreamGetResponse, GetStreamApiTasksTaskIdStreamGetError, InfiniteData<GetStreamApiTasksTaskIdStreamGetResponse>, QueryKey<Options<GetStreamApiTasksTaskIdStreamGetData>>, number | Pick<QueryKey<Options<GetStreamApiTasksTaskIdStreamGetData>>[0], 'body' | 'headers' | 'path' | 'query'>>(
    // @ts-ignore
    {
        queryFn: async ({ pageParam, queryKey, signal }) => {
            // @ts-ignore
            const page: Pick<QueryKey<Options<GetStreamApiTasksTaskIdStreamGetData>>[0], 'body' | 'headers' | 'path' | 'query'> = typeof pageParam === 'object' ? pageParam : {
                query: {
                    after: pageParam
                }
            };
            const params = createInfiniteParams(queryKey, page);
            const { data } = await getStreamApiTasksTaskIdStreamGet({
                ...options,
                ...params,
                signal,
                throwOnError: true
            });
            return data;
        },
        queryKey: getStreamApiTasksTaskIdStreamGetInfiniteQueryKey(options)
    });
    return opts as Omit<typeof opts, 'initialData'>;
};

export const listWorkflowsApiWorkflowsGetQueryKey = (options?: Options<ListWorkflowsApiWorkflowsGetData>) => createQueryKey('listWorkflowsApiWorkflowsGet', options);

/**
 * Every workflow this server can run
 *
 * The registered workflows, by name.
 *
 * What the New Run overlay's workflow chips and the library's left list
 * are built from (10 §Overlays). A server with nothing registered
 * answers with an empty list: it runs no workflows, which is a fact
 * rather than a failure.
 */
export const listWorkflowsApiWorkflowsGetOptions = (options?: Options<ListWorkflowsApiWorkflowsGetData>) => queryOptions<ListWorkflowsApiWorkflowsGetResponse, ListWorkflowsApiWorkflowsGetError, ListWorkflowsApiWorkflowsGetResponse, ReturnType<typeof listWorkflowsApiWorkflowsGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await listWorkflowsApiWorkflowsGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: listWorkflowsApiWorkflowsGetQueryKey(options)
});

export const getWorkflowApiWorkflowsNameGetQueryKey = (options: Options<GetWorkflowApiWorkflowsNameGetData>) => createQueryKey('getWorkflowApiWorkflowsNameGet', options);

/**
 * One workflow
 *
 * One registered workflow, or 404 `unknown_workflow`.
 */
export const getWorkflowApiWorkflowsNameGetOptions = (options: Options<GetWorkflowApiWorkflowsNameGetData>) => queryOptions<GetWorkflowApiWorkflowsNameGetResponse, GetWorkflowApiWorkflowsNameGetError, GetWorkflowApiWorkflowsNameGetResponse, ReturnType<typeof getWorkflowApiWorkflowsNameGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getWorkflowApiWorkflowsNameGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getWorkflowApiWorkflowsNameGetQueryKey(options)
});

/**
 * Submit a run
 *
 * Queue a run of this workflow, and return its id.
 *
 * The run is `queued` rather than `running`: waiting for a slot is a
 * state an operator can see, and the first claim of one of its tasks is
 * what flips it (03 §Run).
 */
export const submitRunApiWorkflowsNameRunsPostMutation = (options?: Partial<Options<SubmitRunApiWorkflowsNameRunsPostData>>): UseMutationOptions<SubmitRunApiWorkflowsNameRunsPostResponse, SubmitRunApiWorkflowsNameRunsPostError, Options<SubmitRunApiWorkflowsNameRunsPostData>> => {
    const mutationOptions: UseMutationOptions<SubmitRunApiWorkflowsNameRunsPostResponse, SubmitRunApiWorkflowsNameRunsPostError, Options<SubmitRunApiWorkflowsNameRunsPostData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await submitRunApiWorkflowsNameRunsPost({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getSourceApiWorkflowsNameSourceGetQueryKey = (options: Options<GetSourceApiWorkflowsNameSourceGetData>) => createQueryKey('getSourceApiWorkflowsNameSourceGet', options);

/**
 * The Python a workflow is defined in
 *
 * The module a workflow's nodes are defined in, and their line numbers.
 *
 * The file is the one the **start node's** body is defined in — the
 * module the author wrote the workflow in — and ``nodes`` carries a line
 * for each node whose body is defined in that same file. A node whose
 * function was imported from elsewhere is left out rather than given a
 * line into text that does not contain it (01 §Real data only).
 *
 * A workflow whose source Python cannot produce — a body built by
 * ``exec``, or one whose file has been deleted since import — is a 404:
 * the workflow is registered, but this view of it does not exist.
 */
export const getSourceApiWorkflowsNameSourceGetOptions = (options: Options<GetSourceApiWorkflowsNameSourceGetData>) => queryOptions<GetSourceApiWorkflowsNameSourceGetResponse, GetSourceApiWorkflowsNameSourceGetError, GetSourceApiWorkflowsNameSourceGetResponse, ReturnType<typeof getSourceApiWorkflowsNameSourceGetQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await getSourceApiWorkflowsNameSourceGet({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getSourceApiWorkflowsNameSourceGetQueryKey(options)
});

/**
 * Model Context Protocol server (streamable HTTP)
 *
 * The agent surface as MCP tools: `get_task`, `append_log`, `submit_result`, and — when the attempt's `ask_policy` is `http` — `ask_operator` and `wait_answer`. Authenticated by the same `X-Athanore-Token` header as `/api/agent/`; the task is the token's, so no tool takes a task id. The request and response bodies are MCP's own JSON-RPC and are opaque to this document.
 */
export const mcpAgentMutation = (options?: Partial<Options<McpAgentData>>): UseMutationOptions<unknown, McpAgentError, Options<McpAgentData>> => {
    const mutationOptions: UseMutationOptions<unknown, McpAgentError, Options<McpAgentData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await mcpAgent({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
